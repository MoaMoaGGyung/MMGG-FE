version: "3.9"
services:
  app:
    container_name: my_app
    build:
      context: .
      dockerfile: dockerfile
    ports:
      - "80:80"
    volumes:
      - .:/app
    command: ["yarn", "preview", "--host"]
